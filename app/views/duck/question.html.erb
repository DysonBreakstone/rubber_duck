<!DOCTYPE html>
<html>
<head>
  <title>The all-knowing duck</title>

  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
    }

    .photo {
      margin-top: 20px;
      width: 50vw;
      height: auto;
    }

    textarea {
      width: 500px;
      height: 100px;
      font-size: 24px; 
      resize: vertical;
      overflow-y: auto; 
      white-space: normal; 
      word-wrap: break-word; 
    }
  </style>
</head>
<body>
  <div class="container">
    <p><h1>Tell the duck what's wrong.</h1></p>

    <img src="<%= @duck.url %>" alt="Photo" class="photo" title="This duck will help you solve your problems. Confess to the duck.">


    <% form_with(url: "/duck/answer", method: :get, local: true) do |form| %>
      <%= form.hidden_field(:duck,value=@duck) %>

      <textarea name="photo_description" value="This duck will help you solve your problems."></textarea>

      <input type="submit" value="Submit">
    </form>
  </div>
</body>
</html>